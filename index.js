import chalk from "chalk"
import _ from "lodash"

console.log(chalk.blue('Hello, Danil\n Nice to see you back!'));
console.log(_.last(['one', 'two']));
const fn   = (a, b) => {

     const c = a + b; return c;
   }
